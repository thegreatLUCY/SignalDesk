# Screenshots

Drop PNGs here with these exact filenames — the main README references them.
(Claude can't capture the running browser UI, so these are taken by hand.)

Lowercase filenames, exactly as below — the main README references them.
(Claude can't capture the running browser UI, so these are taken by hand.)

**Present & shown in the README:**

| filename | what to capture |
|---|---|
| `dashboard.png` | Full dashboard: sidebar + one open chart with overlays. |
| `briefing.png` | Daily Briefing bar **expanded** — risk chip + provenance badge + body. |
| `watchlist.png` | The sidebar alone: grouped Equities/Crypto, price + % per row. |
| `compare.png` | Two charts side-by-side (compare mode, `?a=…&b=…`). |
| `signals.png` | A chart with the legend bar visible (overlays on, MA/Bollinger lines). |
| `macro.png` | Top dock **Macro** tab — FRED pill grid (10Y/2Y/curve/Fed funds/unemp/CPI). |
| `news.png` | Top dock **News** tab — headline list (CNBC/WSJ/Fed) + the descriptive brief. |

**Optional extras** (add the file, then add a row to the README grid):

| filename | what to capture |
|---|---|
| `journal.png` | Bottom Desk dock on **Journal** — a closed trade with its computed P/L chip. |
| `notes.png` | Bottom Desk dock on **Notes** — note list rail + markdown editor. |
| `annotation.png` | Briefing showing a `tier 2 · codex/claude` annotation card. |

Top dock = 3-tab desk (**Briefing | Macro | News**); bottom Desk dock =
**Journal | Notes**. Recommended ~1600px wide, PNG.
