# Screenshots

Drop PNGs here with these exact filenames — the main README references them.
(Claude can't capture the running browser UI, so these are taken by hand.)

## ⚠ Stale — please re-capture (UI has changed since these were taken)

The sidebar, briefing card, and chart-pane header were all restyled
(Pencil-driven). The first five must be re-shot to match the live UI:

| filename | what to capture (CURRENT UI) |
|---|---|
| `dashboard.png` | Full dashboard with the new sidebar (brand tile + LIVE pill + Compare pill) + one open chart with overlays. |
| `briefing.png` | Top dock open on the **Briefing** tab. Show: `Risk tone:` headline + the **4 metric tiles** (`Trend · Breadth · Vol · Crypto F&G`) + at least one badged annotation line (`TIER-2` or `NOTE`) below them. |
| `watchlist.png` | The sidebar alone (full width): brand tile + LIVE pill + Compare + `EQUITIES` / `CRYPTO` uppercase headers with row counts + symbol rows showing `RSI`/`ATR` sub-line + price/% on the right. |
| `compare.png` | Two charts side-by-side (compare mode on; `?a=…&b=…`) with the **new larger header** (slot tile + big symbol + big price + `1M/3M/6M/1Y/MAX` range). |
| `signals.png` | One chart with the new header + the legend bar visible (overlays toggled on: MA20/50/Boll/52w lines). |

## ✓ Still match the live UI (no need to re-shoot)

| filename | what's in it |
|---|---|
| `macro.png` | Macro tab — FRED pill grid. |
| `news.png` | News tab — headline list + descriptive brief. |

## New shot worth adding

| filename | what to capture |
|---|---|
| `picker.png` | Sidebar with the **`+` Add picker** popover open over the rail, showing a few hidden assets (e.g. type `m` to surface `MSFT`, `META`, `MSTR`). Demonstrates the new hidden-asset feature. |

## Optional extras (not in the README grid yet — add the row when you add the file)

| filename | what to capture |
|---|---|
| `journal.png` | Bottom Desk dock on **Journal** — a closed trade with its computed P/L chip. |
| `notes.png` | Bottom Desk dock on **Notes** — note list rail + markdown editor. |
| `annotation.png` | Briefing card showing a `TIER-2` badged annotation line. |

Recommended: ~1600px wide, PNG. Light editing (cropping window chrome) is fine.

After dropping the PNGs in this folder, just say "captured" — I'll
verify filenames, add `picker.png` to the README grid, and push.
